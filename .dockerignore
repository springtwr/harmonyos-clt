# ================================================================
# HarmonyOS Command Line Tools Docker 镜像 - .dockerignore
# ----------------------------------------------------------------
# 作用：排除构建上下文中非必要文件，减小镜像构建上下文体积、
#       加快构建速度。
# 重要：不要排除 CLT 压缩包本身（默认 commandline-tools-linux-x64.zip），
#       因为 Dockerfile 通过 COPY 将压缩包传入镜像。此文件刻意未使用
#       *.zip 通配规则，以确保压缩包始终可用。
# ================================================================

# --- Git 元数据 ---
.git/
.gitignore

# --- 规格与设计文档（构建镜像时不需要）---
spec/
docs/

# --- 说明文档 ---
README.md

# --- 编辑器 / IDE / 系统临时文件 ---
.vscode/
.idea/
*.iml
*.swp
*.swo
.DS_Store
Thumbs.db
